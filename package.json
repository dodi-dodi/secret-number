{
  "name": "secret-number",
  "version": "1.0.0",
  "description": "Client tries to guess server's secret number",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Dorota Durlej",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.2"
  }
}
